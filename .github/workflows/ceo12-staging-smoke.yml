name: CEO-12 staging smoke

on:
  push:
    branches:
      - elpapijuaco325/ceo-12-crear-un-ambiente-de-pruebas-separado-de-produccion

permissions:
  contents: read
  id-token: write

jobs:
  staging:
    uses: ./.github/workflows/firebase-release.yml
    with:
      promote_to_production: false
    secrets: inherit
